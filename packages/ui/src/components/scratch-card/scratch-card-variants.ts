import { cva } from 'class-variance-authority'

export const scratchCardVariants = cva(
    [
        'relative',
        'border-3 border-brutal',
        'rounded-brutal',
        'overflow-hidden',
        'bg-brutal-bg',
    ]
)
