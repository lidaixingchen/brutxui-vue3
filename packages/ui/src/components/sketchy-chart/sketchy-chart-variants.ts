import { cva } from 'class-variance-authority'

export const sketchyChartVariants = cva(
    [
        'border-3 border-brutal',
        'rounded-brutal',
        'bg-brutal-bg',
        'p-4',
    ]
)
