# 样式与架构优化机会审查报告

> 报告类型：发散性审查（基于"阴影过渡与焦点体系统一方案"提炼的优化范式全库扫描）
> 日期：2026-08-12
> 方法：只读扫描——dist 产物审计 + 三个并行维度子 agent 扫描（死导出 / 类名拼接与 transform 变量 / 样式约定违反），均未修改任何文件
>
> **修订记录（2026-08-12）**：经对码复核修正——① 第一章 border-3 实为 3 条规则 + `.border-brutal` 配套，核心风险（单独 border-3 无边框）经产物属性级联核验**不成立**，降级为 P2 同值冗余；② 第四章 4.1 增补产物字节序实证，4.2 修正"阴影正常放大"细节（实为 ON 态反馈整体归零）；③ 第二章 select"已迁"失实（[select-variants.ts:15](../../packages/ui/src/components/select/select-variants.ts#L15) 仍保留手写位移焦点），计数补全为 11 处；④ 第三章 Result.vue:84 为文字色移出、3.1 字面量修正、新增 3.4 漏报（TreeViewNode）、补充测试耦合；⑤ 第六章 动画预设 14 个（死 13）、`useDevtools`/`mergeLocale` 主入口/`lib/index.ts` 三项确认坐实、`createCustomTheme` 补充、死导出定性改为"公共 API 面变更需评审"；⑥ 第七章 formToggle 共享实际仅 checkbox+switch、树版无位移；⑦ 第八章 修复模板需扩展 `background-color`（原模板只覆盖文字色）；⑧ 第九章 menu 无 roving focus、Transfer 对照侧修正；⑨ 优先级表新增 P2-评审档并调整 border-3 定级。
>
> **补强记录（2026-08-12，并行补审）**：针对本报告未覆盖的基座区域（跨包令牌契约 / 构建链路漂移 / composables 生命周期 / lib 共享层）启动 4 个并行只读子 agent 扫描，新增第十~十三章共 30 项发现（P1×6、P2×23、P2-评审×1）；其中阴影 `@utility` 与 `SHADOW_ENTRIES` 双来源（11.2）为**已评审决策项**（对应主会话 #14，见 [../plans/阴影过渡与焦点体系统一方案.md](../plans/阴影过渡与焦点体系统一方案.md)，保持方案 A 手写 @utility），独立复现但不重复计入待办。

## 背景与优化范式

已实施的"阴影过渡与焦点体系统一方案"（[../plans/阴影过渡与焦点体系统一方案.md](../plans/阴影过渡与焦点体系统一方案.md)）揭示了 8 个可复用的优化范式，本报告按这些范式发散扫描全库，定位同类问题：

1. **手写类 vs Tailwind 生成类双定义**——`@layer utilities` 手写类与 `@theme` 令牌派生类并存（shadow-brutal 即死于此）
2. **焦点/激活体系并行分裂**——同一语义多种实现（ring/outline/背景高亮/位移）
3. **主题化路径绕过 token**——硬编码值替代 `--brutal-*` 派生类
4. **Tailwind v4 变量机制竞态**——`--tw-*` 变量多源写入，胜出者由编译顺序决定
5. **@source 扫描约定违反**——动态拼接类名导致产物静默缺类
6. **死代码/死导出**——定义并导出但零消费者
7. **并行体系残留**——同一语义两套实现
8. **过渡声明与实际属性错配**——`transition-[transform,box-shadow]` 吞掉真实颜色过渡

---

## 一、手写类 vs Tailwind 生成类双定义

### `border-3` 三定义竞态（实为同值冗余，非 shadow-brutal 同源风险）

dist 产物（`packages/ui/dist/styles.css`）中存在 **3 条 `.border-3` 规则**（另有 1 条 `.border-brutal` 配套规则）：

| 规则 | 来源 | 特点 |
| --- | --- | --- |
| `.border-3{border-style:var(--tw-border-style);border-width:var(--border-width-3)}` | Tailwind 生成（`@theme` 的 `--border-width-3` 派生） | 声明 border-style |
| `.border-3{border-width:var(--border-width-3)}` | [styles.css](../../packages/ui/src/styles.css) `@layer utilities` 手写 | 仅声明 border-width，未声明 border-style |
| `.border-brutal{border-style:solid;border-color:var(--brutal-border-color,#000)}` | 手写 | 组件配套提供 solid + token 边框色 |
| `.border-3{border-width:4px}` | `prefers-contrast: high` 全局覆盖（未入层，位于产物末尾） | 高对比度增强 |

**澄清（经产物字节序与属性级联核验）**：两条 `.border-3` 规则**同时作用于同一元素、按属性级联**，而非"后者整体覆盖前者"。手写版只声明 `border-width`（与 Tailwind 版**同值** `var(--border-width-3)`），`border-style` 仍由 Tailwind 版规则的 `border-style:var(--tw-border-style)` 提供，而 preflight 将 `--tw-border-style` 默认置为 `solid`。因此：

- **单独挂 `border-3`（不配 `border-brutal`）时边框正常显示**——全库裸用 `border-3` 的组件（MenuItem、SubMenu、textarea、hardcore-input、spinner、accordion、DataTableColumnFilter、date-picker）实测均为实线边框
- prefers-contrast 的 4px 覆盖未入层且位于产物末尾，**恒胜，高对比度增强生效**
- 手写版对视觉**零贡献、100% 冗余**——真正的性质是"同值双定义"，与 shadow-brutal 之死（手写版为**不同值**、破坏主题）**不同源**，无实际风险

**修复方向**：删手写版，保留 Tailwind 生成版（自带 `border-style`，更完整）。由于两条规则 `border-width` 同值、`border-style` 本由 Tailwind 版提供，删除为**纯冗余清理、无任何行为变更**（裸 `border-3` 删除前后均为实线）。定级为 **P2 冗余清理**，非 P1 架构债。

---

## 二、焦点体系残留（方案 B 延伸）

方案 B（ring→outline 统一）已落地，但全库仍有 11 处与"禁止任何 `outline-none`"约定（[COMPONENT_GUIDE](../../docs/guides/COMPONENT_GUIDE.md)）冲突的焦点处理：

| 位置 | 现状 | 判定 |
| --- | --- | --- |
| [cascader-variants.ts:13](../../packages/ui/src/components/cascader/cascader-variants.ts#L13) | trigger 用 `focus:shadow-brutal-lg` + 位移做焦点指示（含 `focus:outline-none`） | **违反**——但注意：[select-variants.ts:15](../../packages/ui/src/components/select/select-variants.ts#L15) **并非"已迁"**，仍保留完全同款的手写 `focus:shadow-brutal-lg + 位移`，仅额外叠加 `FOCUS_OUTLINE_CLASSES`。cascader 与 select 在手写位移维度一致，唯一差异是 cascader 多了 `focus:outline-none`；select 实为**双焦点指示并存**（outline + 位移），迁移方向需先定设计决策（保留 outline 还是位移） |
| [command-variants.ts:16](../../packages/ui/src/components/command/command-variants.ts#L16)（CommandItem）、[select-variants.ts:51](../../packages/ui/src/components/select/select-variants.ts#L51)（Item）、[MenuItem.vue:54](../../packages/ui/src/components/menu/MenuItem.vue#L54)、[SubMenu.vue:131](../../packages/ui/src/components/menu/SubMenu.vue#L131)、[cascader-variants.ts:44](../../packages/ui/src/components/cascader/cascader-variants.ts#L44)、[popover-variants.ts:9](../../packages/ui/src/components/popover/popover-variants.ts#L9) | 列表项 `outline-none`（command item 非"仅背景高亮"，实为 `brutalHighlightLiftWithBorder` 的背景+阴影+位移+边框四合一） | **违反**——Tailwind v4 `--tw-outline-style` 变量机制下 outline-none 抑制渲染；dropdown-menu 的 `FOCUS_OUTLINE_CLASSES` 是现成迁移模板。⚠️ 方向需设计决策：库内"硬阴影位移"焦点本就是 brutalist 设计语言，与 outline ring 的取舍未经论证，不宜直接全体迁移 |
| [CommandInput.vue:76](../../packages/ui/src/components/command/CommandInput.vue#L76) | input 容器**无任何 focus-within 指示** | **a11y 缺口**——键盘焦点整体不可见，与 select 修复前同类 |
| [input-variants.ts:36](../../packages/ui/src/components/input/input-variants.ts#L36)、[number-input-variants.ts:63](../../packages/ui/src/components/number-input/number-input-variants.ts#L63)、[TagsInputInput.vue:15](../../packages/ui/src/components/tags-input/TagsInputInput.vue#L15) | 容器内 input 的 `focus:ring-0` / `focus:outline-none` | **遗物**——ring 体系残留痕迹（无害但应清理；容器本身已有 `focus-within:outline-*`） |

**计数口径**：上述位置合计 **11 处**（cascader trigger / cascader item / command item / select item / MenuItem / SubMenu / popover / CommandInput / input / number-input / TagsInputInput）。
**测试耦合**：[tags-input.test.ts:77](../../packages/ui/src/components/tags-input/tags-input.test.ts#L77) 断言 `focus:outline-none` 类名，迁移后需同步。

---

## 三、主题化绕过 token（约 20 处，theme-pastel/mono/warm/高对比下失效）

> 机制核验：`--shadow-brutal` 默认 `4px 4px 0px 0px var(--brutal-shadow-color,#000)`，theme-pastel/mono/warm 均通过 `--brutal-shadow-color`/`--brutal-border-color` 覆盖阴影/边框色。以下硬编码值不跟随主题。

### 3.1 手抄 shadow 值

`shadow-[Npx_Npx_0px_0px_rgba(0,0,0,1)]` + `dark:shadow-[...rgba(255,255,255,1)]`——`--shadow-brutal` 令牌值的硬编码（字面量因位而异：6px/4px/3px/2px/1px，[Image.vue:356](../../packages/ui/src/components/image/Image.vue#L356) 为 `rgba(239,68,68,0.3)` 红色）。theme-pastel/mono 只改 `--brutal-shadow-color`，这些元素不跟随：

- [Counter.vue:250](../../packages/ui/src/components/counter/Counter.vue#L250)、[Result.vue:55,69](../../packages/ui/src/components/result/Result.vue#L55-L69)、[MessageContainer.vue:37](../../packages/ui/src/components/message/MessageContainer.vue#L37)、[Tour.vue:389,401,411,418](../../packages/ui/src/components/tour/Tour.vue#L389-L418)、[Image.vue:349,356](../../packages/ui/src/components/image/Image.vue#L349-L356)、[Calendar.vue:288](../../packages/ui/src/components/calendar/Calendar.vue#L288)、[directives/loading.ts:23](../../packages/ui/src/directives/loading.ts#L23)、[Loading.vue:101](../../packages/ui/src/components/loading/Loading.vue#L101)

### 3.2 手抄 `border-brutal-black` + 双 token 并行

`border-brutal-black dark:border-white`——`--brutal-black` 与 `--brutal-border-color` 是两个独立 token 走两套值，theme-pastel 改后者不改前者，边框脱轨：

- [Tour.vue:389,391,397,401-418](../../packages/ui/src/components/tour/Tour.vue#L389-L418)、[Result.vue:55,69](../../packages/ui/src/components/result/Result.vue#L55-L69)、[Counter.vue:250](../../packages/ui/src/components/counter/Counter.vue#L250)、[directives/loading.ts:23](../../packages/ui/src/directives/loading.ts#L23)、[Loading.vue:101](../../packages/ui/src/components/loading/Loading.vue#L101)
- 核验修正：[Result.vue:84](../../packages/ui/src/components/result/Result.vue#L84) 原列于本组，实际是 `text-brutal-black dark:text-white`（**文字色**非边框），已移出

### 3.3 硬编码色

- [Result.vue:30,38](../../packages/ui/src/components/result/Result.vue#L30-L38)：`bg-[#22c55e]` / `bg-[#3b82f6]`——库内已有 `--brutal-success` / `--brutal-info` token 未用
- [Result.vue:93](../../packages/ui/src/components/result/Result.vue#L93)、[Counter.vue:255](../../packages/ui/src/components/counter/Counter.vue#L255)：`text-gray-500 dark:text-gray-400`——`--brutal-muted-foreground` 的手抄

### 3.4 漏报补充

- [TreeViewNode.vue:84,87,90,300](../../packages/ui/src/components/tree-view/TreeViewNode.vue#L84-L90)：拖拽 drop 指示与 retry 按钮的 `border-black dark:border-white` / `bg-black/5`——绕过 `--brutal-border-color`

**违反** VISUAL_SYSTEM 规则 2/6（仅使用 `shadow-brutal*`、禁止硬编码颜色）。
**测试耦合**：[counter.test.ts:100-101](../../packages/ui/src/components/counter/counter.test.ts#L100-L101) 断言 `border-brutal-black` / `shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]` 类名，修复后需同步。

---

## 四、Tailwind v4 transform 变量竞态（`--tw-translate-y` 多源写入）

产物 CSS 字节序（同特异度后者胜）：`hover:*` < `focus:*` < `active:*` < `data-[highlighted]:*` < `data-[state=on]:*` 等，即 **`data-*` 变体恒后于 hover/focus/active**。

### 4.1 command item 按压反馈失效（真实缺陷，已字节序实证）

[command-variants.ts:24-25](../../packages/ui/src/components/command/command-variants.ts#L24-L25)：`brutalHighlightLiftWithBorder`（`data-[highlighted]:-translate-y-0.5` + `data-[highlighted]:shadow-brutal-lg`）与 `brutalPress`（`active:translate-y-[var(--brutal-pressed-offset,2px)]` + `active:shadow-none`）同元素并存。reka Listbox 的 `data-highlighted` 在鼠标按下前必已置位，高亮项按压时两态必同时成立；产物字节序实测 `active:translate-y-...`@80230 < `data-[highlighted]:-translate-y-0.5`@84674、`active:shadow-none`@80763 < `data-[highlighted]:shadow-brutal-lg`@85714，同特异度 (0,2,0) 下 `data-[highlighted]` 恒胜 → **所有鼠标按压场景下按压位移与去影反馈完全不出现**（非偶发）。含 `transition-all` 也救不回——状态切换期间计算值不变。违反 brutalPress"active 整体接管"约定；对照 cascader/select 的 `active:translate-x-0` 显式重置模式，此处未处理。

### 4.2 toggle ON 态持久位移压过 hover/active（需人工确认意图）

[toggle-variants.ts:19,24](../../packages/ui/src/components/toggle/toggle-variants.ts#L19-L24)：`data-[state=on]`（持久 +2px）、`hover`（-2px）、`active`（+2px）三源写入同一变量，`data-[state=on]` 恒胜（产物 @90865）→ **ON 态下 hover 浮起完全不生效**。核验修正：`data-[state=on]:shadow-none`（@91495）同样压过 `hover:shadow-brutal`（@77938），**ON 态 hover 阴影也不会"正常放大"**——实际 ON 态 hover/按压的位移与阴影反馈整体归零（bg/text 亦被 data-on 覆盖），非原述"阴影上浮语义与位置脱节"。与 switch/checkbox 同族组件（checked 位移在独立 thumb）行为不一致。

### 4.3 已核查安全（对照基准）

carousel 居中 calc 保留、cascader/select 的 active 显式重置、Image/Tour/number-input 的 hover/active 成对（active 后置接管成功）、form-toggle-base 的 disabled 特异度压制、date-picker hover 只写 x / active 只写 y（不同变量）、Rate 展平按压、SelectContent 动画走关键帧非变量——均符合 brutalPress 约定。

---

## 五、类名动态拼接（@source 扫描约定违反）

### separator 插值拼接（潜伏缺类，高置信）

[separator-variants.ts:23-24,34](../../packages/ui/src/components/separator/separator-variants.ts#L23-L34)：

```ts
export const DEFAULT_THICKNESS = '3px'
horizontal: `h-[var(--sep-thickness,${DEFAULT_THICKNESS})] w-full`,
```

类名被 `${DEFAULT_THICKNESS}` 插值，源码中无完整字面量，与 [brutal-interaction-variants.ts](../../packages/ui/src/lib/brutal-interaction-variants.ts#L2-L4) 声明的"完整字面量"约定相悖。**横向 `h-[var(--sep-thickness,3px)]` 与纵向 `w-[var(--sep-thickness,3px)]` 两个选择器均无源码字面量，唯一字面量来源都是测试文件** [separator.test.ts:10,42](../../packages/ui/src/components/separator/separator.test.ts#L10-L42)（@source `../**/*.ts` 扫描意外兜底）——测试断言一改或常量一变即静默缺类（高度归零）。

### 已排查安全

CodeBlock 的 `language-${...}`（prismjs 语言类，非 Tailwind 工具类，样式来自组件自带 CSS）；其余 `${...}` 均拼接完整字面量常量（运行类名集合 ⊆ 源码字面量集合）。

---

## 六、死代码与死导出

### 6.1 死导出 18 项（index.ts 再导出但零生产消费者，含确认项）

| 组 | 导出 | 定义位置 | 证据 |
| --- | --- | --- | --- |
| color.ts（5） | `rgbToHsv`/`rgbToHsl`/`hslToRgb`/`hexToHsv`/`hexToRgb` | [lib/color.ts:78-157](../../packages/ui/src/lib/color.ts#L78-L157) | 仅文件内自调用 + color.test.ts |
| date.ts（1） | `getISOWeekNumber` | [lib/date.ts:16](../../packages/ui/src/lib/date.ts#L16) | 仅文件内自调用（formatDate 等）+ date.test.ts |
| themes（8） | `themes`/`defaultTheme`/`darkTheme`/`highContrastTheme`/`minimalTheme`/`getPresetThemeNames`/`getPresetTheme`/`createCustomTheme` | [themes/index.ts:21-277](../../packages/ui/src/themes/index.ts#L21-L277) | **仅 themes.test.ts 引用**——预设主题产品功能在库内零消费者 |
| devtools（3） | `devtoolsPlugin`/`setupDevtools`/`useDevtools` | [lib/devtools-plugin.ts:599-658](../../packages/ui/src/lib/devtools-plugin.ts#L599-L658) | 仅 devtools-plugin.test.ts + 实现内部 app.use |
| locales（1） | `mergeLocale` 主入口 re-export | [index.ts:352](../../packages/ui/src/index.ts#L352) | docs 均经 `brutx-ui-vue/locales` 子路径，主入口零生产消费者 |
| 聚合桶 | `lib/index.ts` | [lib/index.ts](../../packages/ui/src/lib/index.ts) | 整文件零 import（仅 typedoc 文档入口） |

**总计 18 项**（`useDevtools`、`mergeLocale` 主入口、`lib/index.ts` 三项原"需人工确认"已坐实；`createCustomTheme` 为补充）。⚠️ 均为**文档化的公共 API**（主入口 re-export + apps/docs/guide/theme.md、skills/design-tokens.md、package.json 子路径导出），清理属 **API 面变更**，需评审后再动，非纯机械。

### 6.2 动画死预设与零引用

- `animate-brutal-*` **14 个**全局预设（[styles.css:651-703](../../packages/ui/src/styles.css#L651-L703)）**仅 Badge.vue 的 badge-pulse 在用**，其余 **13 个死预设**（核验修正：原 13/12 计数字节偏少）；组件实际用 Tailwind 默认 `animate-pulse/spin/bounce`。⚠️ 同为文档化公共预设（apps/docs/guide/theme.md、skills/design-tokens.md），删除前需评估用户面
- [useAnimation.ts](../../packages/ui/src/composables/useAnimation.ts) 组合式零组件引用——reduced-motion 门控三套并存（styles.css 全局 / useAnimation / 组件手写如 InfiniteScroll.vue:184、HardcoreInput.vue:288）；⚠️ 有 package.json `./useAnimation` 子路径与 docs 文档，属公共 API
- HardcoreInput 的 `animate-shake` 与全局 `animate-brutal-shake` 同一 shake 语义两套实现（组件内 scoped `:deep()` 自定 keyframes）

### 6.3 其他遗物

`focus:ring-0`（input 内层）、`lib/index.ts` 聚合桶零引用。

---

## 七、选中态指示语言分裂（6+ 种并存）

同一"选中/激活态"语义，库内六种以上实现：

| 语言 | 位置 |
| --- | --- |
| 四重组合（bg+文字+边框色+阴影+位移） | [MenuItem.vue:54-58](../../packages/ui/src/components/menu/MenuItem.vue#L54-L58)（含位移）；[tree-variants.ts:20](../../packages/ui/src/lib/tree-variants.ts#L20)（bg+文字+边框色+阴影，**无位移**，与 MenuItem 非完全同款） |
| bg 反转 + 彩色阴影 | [pagination-variants.ts:40-45](../../packages/ui/src/components/pagination/pagination-variants.ts#L40-L45) |
| 边框式 | [tabs-variants.ts:44,51-54](../../packages/ui/src/components/tabs/tabs-variants.ts#L44-L54) |
| bg + 文字色 | [table-variants.ts:70](../../packages/ui/src/components/table/table-variants.ts#L70)（`data-[state=selected]:bg-brutal-accent + text-brutal-accent-foreground`，非"仅 bg"） |
| 组件内三种细分 | [cascader-variants.ts:50-60](../../packages/ui/src/components/cascader/cascader-variants.ts#L50-L60)（active/selected/trail 三种做法） |
| 共享变体 | checkbox + switch 的 `formToggleVariantColors`（[form-toggle-base.ts:23-29](../../packages/ui/src/lib/form-toggle-base.ts#L23-L29)）。⚠️ 核验修正：**radio、toggle 各自手写** `data-[state=checked]/[state=on]` 配色，未共享——分裂实际比清单更多 |

---

## 八、过渡声明与实际属性错配（6 处真实视觉缺陷）

`transition-[transform,box-shadow]`（brutalPressWithTransition）与同元素真实发生的背景/文字颜色变化共存 → **hover 变色无过渡，瞬时跳变**（twMerge 同组去重、后者胜，已用真实类串实证 tailwind-merge 3.6.0）：

| 位置 | 证据 |
| --- | --- |
| [code-block-variants.ts:29](../../packages/ui/src/components/code-block/code-block-variants.ts#L29) | 复制按钮 `brutalPressWithTransition` + `hover:bg-brutal-muted`。⚠️ 此元素**本无 `transition-all`**，非"被覆盖"，而是共享变体主动限定 transition-property 后 bg 变色无过渡（是否属缺陷属设计取舍） |
| [Calendar.vue:132-137](../../packages/ui/src/components/calendar/Calendar.vue#L132-L137) | 日格子 `transition-all duration-100` + hover bg/text/font 变化，随后被 WithTransition 覆盖——hover 变色失去过渡（bg/text/**font-weight** 均不参与） |
| [DatePickerPanel.vue:103-104](../../packages/ui/src/components/date-picker/DatePickerPanel.vue#L103-L104)、[DatePickerRangePanel.vue:124-125](../../packages/ui/src/components/date-picker/DatePickerRangePanel.vue#L124-L125)、[DateTimePickerPanel.vue:135-136](../../packages/ui/src/components/date-picker/DateTimePickerPanel.vue#L135-L136)、[WeekPickerPanel.vue:119-120](../../packages/ui/src/components/date-picker/WeekPickerPanel.vue#L119-L120) | 与 Calendar 日格子完全相同模式，同一缺陷复制 4 处 |
| 自相矛盾 | [MonthPickerPanel.vue:98-100](../../packages/ui/src/components/date-picker/MonthPickerPanel.vue#L98-L100)/[YearPickerPanel.vue:113-115](../../packages/ui/src/components/date-picker/YearPickerPanel.vue#L113-L115) 用不带 WithTransition 的 `brutalPress`，transition-all 保留——与日格子两套口径 |

**修复模板**：[breadcrumb-variants.ts:17](../../packages/ui/src/components/breadcrumb/breadcrumb-variants.ts#L17) 的 `transition-[transform,box-shadow,color]` **只覆盖文字色场景**（breadcrumb hover 仅改 `color`）。本表 6 处实际变化的是 **`background-color` + `font-weight`**，需按场景扩展为 `transition-[transform,box-shadow,background-color,color]` 等；复制按钮（无 transition-all）建议直接改为完整属性列表或评估是否需 bg 过渡。

---

## 九、组件级并行体系

| 并行组 | 两套实现 | 说明 |
| --- | --- | --- |
| 菜单项 | [MenuItem.vue:52-69](../../packages/ui/src/components/menu/MenuItem.vue#L52-L69)（手写激活态状态机 + **全启用项 tabindex=0**、无方向键处理、无 aria-activedescendant） vs [dropdown-menu-variants.ts:17-27](../../packages/ui/src/components/dropdown-menu/dropdown-menu-variants.ts#L17-L27)（reka-ui，roving focus） | 核验修正：menu **并无 roving focus**（无 Arrow 键处理，仅 Enter/Space 可选）——键盘导航缺失比"手写 roving focus"更甚，a11y 差距更大 |
| 通知表面 | [MessageContainer.vue:37](../../packages/ui/src/components/message/MessageContainer.vue#L37)（border-2 + 硬编码阴影） vs [toast-variants.ts:3-9](../../packages/ui/src/components/toast/toast-variants.ts#L3-L9)（border-3 + shadow-brutal-lg） | 边框宽度与阴影路径双轨（并入第三/八节） |
| 选项列表 | [Transfer.vue:203,293](../../packages/ui/src/components/transfer/Transfer.vue#L203-L293)（1px 细边框 `border` + 默认 `rounded`） vs select/cascader 选项（[select-variants.ts:48-54](../../packages/ui/src/components/select/select-variants.ts#L48-L54) **无边框**；[cascader-variants.ts:44-45](../../packages/ui/src/components/cascader/cascader-variants.ts#L44-L45) `border-3 border-transparent` + `rounded-brutal`，默认无 shadow） | Transfer 疑似历史遗留；核验修正：对照侧"border-3 + rounded-brutal + shadow"仅 cascader 部分成立（默认无 shadow），select 无边框 |

---

## 十、跨包令牌契约：三套主题系统与 CLI 静态 CSS 漂移

`packages/shared/src/design-tokens.ts` 是全库令牌唯一事实来源，但存在两条**绕开它**的路径：CLI 的静态 CSS 手抄副本、ui 内两套 JS 主题系统手抄副本。

### 10.1 CLI 静态 CSS 实际漂移（P1，已核）

[brutalist.css:57](../../packages/cli/src/styles/brutalist.css#L57) 的 `--brutal-info-foreground: #ffffff` 与唯一事实来源不符——[design-tokens.ts:81](../../packages/shared/src/design-tokens.ts#L81)/[styles.css:96](../../packages/ui/src/styles.css#L96) 均为 `#000000`（黑字 6.28:1 过 WCAG AA）。dark 块同理（CLI:86 `#ffffff` vs 源 `#000000`）。该文件是 846 行**无任何生成标记的纯手写副本**，由 [constants.ts:176](../../packages/cli/src/lib/constants.ts#L176) 读取注入用户项目；shared 变更不波及它，`audit-brutal-fallback.ts` 只扫 `packages/ui/src` 覆盖不到，无门禁拦截。

### 10.2 CLI 令牌覆盖率缺口（P1）

[brutalist.css:34-89](../../packages/cli/src/styles/brutalist.css#L34-L89) 手写令牌区多处缺失：`@theme` 无 `--color-brutal-black/yellow`、`:root` 无 `--brutal-black/yellow`（styles.css:99-100 有）、`.theme-warm` 整块缺失（styles.css:244-297 有）。

> 修复方向（10.1/10.2 合并）：CLI brutalist.css 改由 `generate-styles-tokens.ts` 同源生成或构建时从 ui 产物抽取，并加 CI 比对脚本核对与 `BASE_THEME` 值一致；本次至少先修正 `#ffffff`→`#000000`。

### 10.3 theme-variables.ts 默认主题双来源（P1，已核）

[theme-variables.ts:188-455](../../packages/ui/src/lib/theme-variables.ts#L188-L455) 的 `DEFAULT_THEME/DARK_THEME/PASTEL_THEME/MONO_THEME/WARM_THEME` 整份手抄 `BASE_THEME`（注释"与 themes/index.ts 预设保持一致"），`themeVariablesToCssVars`(:477-525) 又手写 `--brutal-*` 名映射复制 shared `TOKEN_TO_CSS_VAR`。当前值与源一致（已逐项核），但零引用关系——改 shared 只重生成 styles.css，此处会静默陈旧。

### 10.4 第三套主题系统 themes/index.ts（P2）

[themes/index.ts:21-244](../../packages/ui/src/themes/index.ts#L21-L244) 的 `defaultTheme/darkTheme/highContrastTheme/minimalTheme` 值再次手抄 shared；`minimal`/`high-contrast` 在 styles.css 无对应 `.theme-*` 类；`themes.test.ts` 只自引用结构断言，不交叉校验 shared/styles.css。

### 10.5 styles.css `.theme-*` 预设块在生成标记外（P2）

[styles.css:141-297](../../packages/ui/src/styles.css#L141-L297) 的 `.theme-pastel/.theme-mono/.theme-warm` 位于生成标记外（标记只圈定 `@theme` 与 `:root/.dark`），与 theme-variables.ts 的 `PASTEL_THEME/MONO_THEME/WARM_THEME` 双份维护，当前一致但无机制保证后续一致。

### 10.6 ThemeVariables 类型与 shared ThemeTokens 平行（P2）

[theme-variables.ts:19-124](../../packages/ui/src/lib/theme-variables.ts#L19-L124) 的 `ThemeColors/ThemeBorder/ThemeShadow/ThemeVariables` 是手写平行接口，与 `ThemeTokens` 字段语义 20 项重叠但结构不同（嵌套 vs 扁平），无类型层对齐；shared 增删 token 此处静默不跟随。另 `themeVariablesToCssVars`(:511-523) 输出的 `--brutal-spacing-*`/`--brutal-font-*` 不在 shared 契约内。

### 10.7 theme-fallbacks.ts 硬编码（P2）

[theme-fallbacks.ts:10-20](../../packages/ui/src/lib/theme-fallbacks.ts#L10-L20) 硬编码 `#FF6B6B`/`#4ECDC4`/`#000000`，因 registry:lib 自包含约束无法引用 shared（注释已标注）。建议在 registry 包内加校验测试断言与 `BASE_THEME.light` 相等。

## 十一、构建链路：turbo 缓存漂移门禁失效与生成器校验缺口

### 11.1 turbo 缓存绕过 CI drift 门禁（P1，已核）

[turbo.json:14-23](../../turbo.json#L14-L23) 的 `brutx-ui-vue#build` inputs 未列入 `../../packages/shared/src/**`，且 shared 包无 build script（`main` 直指 `./src/index.ts`），`dependsOn:["^build"]` 不产生哈希链路。只改 design-tokens 的 PR：CI 中 ui build 远程缓存命中 → `prebuild:tokens` 不执行 → `git diff styles.css` 无变化 → 门禁误判通过，styles.css 与令牌源静默漂移。**registry 侧已修**（[turbo.json:31-36](../../turbo.json#L31-L36) 显式加入 shared/ui inputs + `cache:false`），ui build 侧漏了——正踩记忆库 `turbo-cache-cross-package-drift` 记录的坑。

### 11.2 阴影 @utility 与 SHADOW_ENTRIES 双来源（已评审决策，非新增）

补审独立复现了阴影 `@utility` 手写块与 `SHADOW_ENTRIES` 生成区间不一致的漂移风险（对应主会话 #14）。主会话已做设计评审并决策保持方案 A（手写 @utility，拓展性更优），生成方案已回滚（见 [../plans/阴影过渡与焦点体系统一方案.md](../plans/阴影过渡与焦点体系统一方案.md)）。此处仅记录"独立复现"事实，不重复计入待办；若未来阴影档位变更频率上升，可再评估纳入生成或加断言。

### 11.3 生成器校验缺口组（P2）

- [generate-styles-tokens.ts:178-201](../../packages/ui/scripts/generate-styles-tokens.ts#L178-L201)：无 `--check`/dry-run 模式（对比 generate-exports.ts 有 `--check`/`--verify`），唯一验证途径是被 11.1 绕过的 git-diff。
- [audit-brutal-fallback.ts:127-147](../../packages/ui/scripts/audit-brutal-fallback.ts#L127-L147)：只校验 `var(--brutal-*)` **是否带** fallback，不校验 fallback **值**是否等于 `BASE_THEME.light`——写错值也能通过门禁。
- [ci.yml:75-82](../../.github/workflows/ci.yml#L75-L82)：prebuild:scan 同产 `registry-manifest.json` 与 `exports-manifest.json`，门禁只 diff 前者；新增 composable/directive 未重跑 scan 时，manifest 与 package.json exports 一起保持陈旧且自洽，`check:exports` 只比"package.json vs manifest"而非"manifest vs 源码"。
- [generate-exports.ts:98-113,176-179](../../packages/ui/scripts/generate-exports.ts#L98-L113)：`--verify` 只校验自动生成子路径，4 个手工 key（`.`、`./style.css`、`./preflight.css`、`./locales`）不在范围，覆盖它们的 `smoke-package.mjs` 未接入 CI。

### 11.4 字体栈三处硬编码（P2）

[generate-styles-tokens.ts:88](../../packages/ui/scripts/generate-styles-tokens.ts#L88) 与 [preflight.css:16-27](../../packages/ui/src/preflight.css#L16-L27) 逐字重复同一字体栈字面量（styles.css:43 为其生成副本），design-tokens 无 font token。改字体需改两处手写点。

### 11.5 prebuild-scan 与 build-registry 覆写清单重复（P2）

[prebuild-scan.ts:66-70](../../packages/ui/scripts/prebuild-scan.ts#L66-L70) 与 [build-registry.ts:96-100](../../packages/registry/scripts/build-registry.ts#L96-L100) 的 `MANIFEST_OVERRIDES`/`LIB_EXCLUDE` 逐字重复（注释自称"复用 prebuild-scan 逻辑"却是重写非导入）。改一处漏另一处会让 registry watch 产物与 CI 产物静默分叉。

**校验链健康侧**：design-tokens 内 `CSS_VARS` 由 `BASE_THEME` 经 `toCssVars` 派生（design-tokens.ts:164-167），生成脚本 @theme/:root 两处取值实际同源无内部漂移；`injectBetweenMarkers` 幂等且丢弃标记区间内手改；registry 链路 `cache:false` + 透传 shared `CSS_VARS` + 源码直读（非 dist）+ `computeSourceHash` 计入 `CSS_VARS`，无跨包缓存漂移（已抽查 button.json 28 键完整）。

## 十二、composables 生命周期与健壮性（34 个组合式）

### 12.1 KeepAlive 停用后定时器空转（P1）

[useCarousel.ts:71](../../packages/ui/src/composables/useCarousel.ts#L71)、[useCarouselEnhanced.ts:47](../../packages/ui/src/composables/useCarouselEnhanced.ts#L47)、[useGlitchEffect.ts:58](../../packages/ui/src/composables/useGlitchEffect.ts#L58) 三个基于 setInterval 的 composable 只清理于 `onUnmounted`/`onBeforeUnmount`，无 `onDeactivated`/`onActivated`（全库 grep 确认零使用）。宿主被 `<KeepAlive>` 缓存停用后定时器持续空转——隐藏轮播继续 `scrollNext`、隐藏故障效果继续翻转 `isActive`。

### 12.2 useToast fallback 单例无作用域清理（P1）

[useToast.ts:242-249](../../packages/ui/src/composables/useToast.ts#L242-L249) `createToast(true)` 不注册 `onScopeDispose`（:139-143 仅非 fallback 路径注册），toasts 仅靠 `beforeunload` 清空；注释已承认"长生命周期 SPA 经 fallback 创建的 toast 一直残留"。修复方向：仿 useTheme fallback 的引用计数式清理，或文档化后接受。

### 12.3 兄弟 composable 行为不一致（P2）

- [useThrottle.ts:140-144](../../packages/ui/src/composables/useThrottle.ts#L140-L144) 无 `disposed` 守卫，卸载后外部残留引用调用会立即执行 fn；兄弟 [useDebounce.ts:55](../../packages/ui/src/composables/useDebounce.ts#L55) 专门加了 `disposed` 并注释——两兄弟行为不一致。
- [useDebounce.ts:109](../../packages/ui/src/composables/useDebounce.ts#L109) `onUnmounted` 无 `getCurrentInstance()` 守卫（useThrottle 有），非组件上下文调用触发 Vue"无活动实例"警告。
- [useCarousel.ts:85](../../packages/ui/src/composables/useCarousel.ts#L85) `autoplayDelay<=0` 无守卫，setInterval 以浏览器下限疯狂滚动；增强版 [:40](../../packages/ui/src/composables/useCarouselEnhanced.ts#L40) 已有守卫。

### 12.4 重复实例化与监听残留（P2）

- [useCarouselEnhanced.ts:20](../../packages/ui/src/composables/useCarouselEnhanced.ts#L20) 自身调用 useReducedMotion，而内部 useCarousel 已实例化一次——同一组件两条 matchMedia 查询与监听（无泄漏但冗余，若宿主再调则三份）。
- [useTheme.ts:247-249](../../packages/ui/src/composables/useTheme.ts#L247-L249)/[useToast.ts:226-228](../../packages/ui/src/composables/useToast.ts#L226-L228) 模块顶层 `beforeunload` 监听从不移除，`destroyBrutxUI()` 只清单例不清监听，HMR/多应用同页累积。

### 12.5 魔法数字与硬编码（P2）

[useClipboard.ts:49](../../packages/ui/src/composables/useClipboard.ts#L49) `100` 裸写（`DEFAULT_COPIED_DURATION` 已是命名常量）；[useSelectionDisplayText.ts:69](../../packages/ui/src/composables/useSelectionDisplayText.ts#L69) `?? 3` 裸写、:76 默认计数文案英文硬编码；[useFormFieldValidation.ts:56](../../packages/ui/src/composables/useFormFieldValidation.ts#L56) `'Invalid value'` 英文默认（zhCN 默认环境不落地）。

**健康侧**：清理纪律良好——useReducedMotion/useDialog/useDialogEnhanced/useCanvasInteraction/useKanban/useAudioEngine 均对称清理；SSR 守卫体系（lib/env.ts）完备；watch 依赖未发现循环。存疑 2 项未计入（useCanvasInteraction 条件渲染时 ResizeObserver 建立时机、useDialogEnhanced close 竞态）。

## 十三、lib 共享层死代码、重复实现与输入校验

### 13.1 CSS 变量主题系统零生产消费者（P2-评审）

[theme-variables.ts:706,890,916](../../packages/ui/src/lib/theme-variables.ts#L706)/[theme-editor.ts:292](../../packages/ui/src/lib/theme-editor.ts#L292) 的 `createThemeVariables/createDarkModeToggle/createThemeEditor/DEFAULT_THEMES` 整系统约 1600 行**零生产消费者**（全仓仅测试 + 死聚合桶 `lib/index.ts` 引用，主入口 index.ts 未导出）——未被既有报告死导出 18 项覆盖，属新增。⚠️ 均为文档化公共 API（子路径/指南），清理属 API 面变更需评审。

### 13.2 共享交互变体死代码（P2）

[brutal-interaction-variants.ts:29](../../packages/ui/src/lib/brutal-interaction-variants.ts#L29) `brutalHoverLiftWithTransition`、[:33](../../packages/ui/src/lib/brutal-interaction-variants.ts#L33) `brutalPressWithShadowSm` 零消费者（对称兄弟 `brutalPressWithTransition` 6 处使用）；[:18](../../packages/ui/src/lib/brutal-interaction-variants.ts#L18) `brutalHighlightLift` 与 [modal-variants.ts:19](../../packages/ui/src/lib/modal-variants.ts#L19) `CLOSE_BUTTON_BASE_CLASSES` 仅内部组装无外部 import——改私有即可，不改变行为。

### 13.3 重复实现（P2）

- [theme-editor.ts:146-198](../../packages/ui/src/lib/theme-editor.ts#L146-L198) 与 [theme-variables.ts:477-525](../../packages/ui/src/lib/theme-variables.ts#L477-L525)：同一份 ThemeVariables→CSS 变量映射实现两遍（键列表逐字重复，仅差 prefix 参数）。
- 深合并逻辑三份：[theme-editor.ts:102](../../packages/ui/src/lib/theme-editor.ts#L102)、[theme-variables.ts:568](../../packages/ui/src/lib/theme-variables.ts#L568)、[themes/index.ts:309](../../packages/ui/src/themes/index.ts#L309)。
- [Image.vue:372,384,396](../../packages/ui/src/components/image/Image.vue#L372)：三处内联手写 `brutalHoverLiftNoX`+`brutalPress`+`FOCUS_OUTLINE_CLASSES` 等义类串，未 import 共享常量（共享层恰为其单一来源）。

### 13.4 theme-editor `__proto__` 键污染（P2 潜伏）

[theme-editor.ts:428,453,556,627](../../packages/ui/src/lib/theme-editor.ts#L428) 以外部可控名（`importTheme` 任意 name、`importThemeFromFile` 文件名、`importAllThemes` JSON 键）赋值 `themes[name] = structuredClone(...)`，未防 `__proto__`/`constructor`/`prototype`；同子系统 theme-variables.ts:573、themes/index.ts:318、devtools-plugin.ts:440 均显式跳过，唯独此处漏。一个 `__proto__.json` 文件即可污染内部主题表原型。

**健康侧**：String.replace 特殊模式全库无风险（date.ts 替换值为数字串、`'\$&'` 系有意转义）；locale en/zh-CN/types 三层 325 键逐项一致无漂移；`Z_INDEX`/`iconSizeVariants`/`chip-variants`/`tree-variants`/`floating-*` 均有实际消费者。

## 优先级汇总

| 级别 | 内容 | 理由 |
| --- | --- | --- |
| **P0 真实缺陷** | ① command 按压反馈失效（4.1）② 过渡错配 6 处（八）③ separator 潜伏缺类（五） | 交互/视觉已坏或随时会坏，修复面小；② 模板需含 `background-color`，③ 修复需同步测试文件中的字面量 |
| **P1 架构债** | ① 主题化绕过约 20 处（三）② 焦点体系残留 11 处（二）③ 跨包令牌漂移：CLI brutalist.css 实际值 + 覆盖率缺口（10.1-10.2）④ theme-variables 双来源（10.3）⑤ turbo 缓存门禁失效（11.1）⑥ composables KeepAlive 定时器 / toast fallback（12.1-12.2） | 与"焦点体系合一"同级的系统性债，延续"合一"主线；② 迁移方向（outline vs 位移焦点）需先定设计决策；③-④ 绕开 shared 单一来源（10.1 已实际漂移），⑤ 正踩 turbo 跨包缓存坑，⑥ 运行期行为缺陷 |
| **P2 清理** | ① border-3 同值双定义（一，无实际危害，纯冗余）② toggle ON 态反馈归零确认（4.2）③ focus:ring-0 / focus:outline-none 遗物 ④ 生成器校验缺口组（11.3-11.5）⑤ composables 兄弟不一致 / 魔法数字（12.3-12.5）⑥ lib 死代码 / 重复 / `__proto__`（13.2-13.4） | 无实际风险或设计问题，机械清理或局部加固 |
| **P2-评审** | 死导出 18 项、动画死预设 13、useAnimation（六）、CSS 变量主题系统约 1600 行（13.1） | 均为文档化公共 API，删除是 API 面变更，需评审后再动，非"风险低" |
| **P3 探索** | 选中态语言统一（七）、menu/reka 双轨（九，含键盘导航 a11y 差距）、transfer 语言（九）、主题系统三套合一（10.4-10.7） | 需要设计决策，非纯机械 |

---

## 附录：扫描范围与方法

1. **dist 产物审计**：`packages/ui/dist/styles.css`（2026-08-12 17:21 构建）规则级比对（border-3 双定义）。⚠️ 该快照略旧于 HEAD（缺 `data-[highlighted]:border-brutal` 变体），字节序结论不受影响，但后续引产物做字节序证据前建议先 `pnpm build` 再比对
2. **死导出扫描**：`packages/ui/src/index.ts` 非组件/非 composable 具名导出逐个全仓 `git grep`（含 apps/docs、packages/registry、packages/cli）
3. **样式约定扫描**（4 子任务）：圆角硬编码 / 边框宽度混用 / 过渡声明错配 / 并行体系残留，依据 VISUAL_SYSTEM.md 与 COMPONENT_GUIDE.md 约定
4. **拼接与 transform 扫描**（2 子任务）：类名 `${}` 插值（@source 完整字面量约定）+ translate 变量多源写入（产物 CSS 字节序判定胜出顺序）
5. **复核口径**：计数以"列表即计数"为准（border-3 为 3 条规则、焦点残留 11 处逐条列出、动画预设 14 个）；涉及类名断言的测试（counter / tags-input / separator）修复时需同步更新
6. **跨包令牌契约扫描**（补强·并行子 agent，只读）：`packages/shared/src/design-tokens.ts` 逐项比对消费方 `packages/cli/src/styles/brutalist.css`、`packages/ui/src/lib/theme-variables.ts`、`packages/ui/src/themes/index.ts`、`packages/ui/src/styles.css` 主题块值；registry 链路核验（turbo.json inputs、build-registry.ts 源码直读、CSS_VARS 透传）
7. **构建链路扫描**（补强·并行子 agent，只读）：`packages/ui/scripts/` 全部 9 脚本 + `preflight.css` + `turbo.json` + `.github/workflows/ci.yml`，核对生成单一来源、幂等性、CI 门禁覆盖
8. **composables 生命周期扫描**（补强·并行子 agent，只读）：`packages/ui/src/composables/` 34 文件，grep 高风险模式（addEventListener/setInterval/requestAnimationFrame/ResizeObserver/onUnmounted）后逐一定位清理路径
9. **lib 共享层发散扫描**（补强·并行子 agent，只读）：`packages/ui/src/lib/`（32 文件）+ themes/locales/directives，逐类核对死代码 / 重复实现 / 双事实来源 / String.replace 陷阱 / 输入校验；避开既有报告已覆盖的死导出清单
