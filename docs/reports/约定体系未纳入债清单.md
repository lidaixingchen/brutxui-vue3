# 约定体系未纳入债清单

> 报告类型：既有债集中清单（约定体系修复方案范围外的组件代码修复项）
> 日期：2026-08-12
> 范围：C1-C8 组件代码修复不在[《约定体系修复方案》](../plans/约定体系修复方案.md)范围内（该方案只修约定体系自身：文档措辞 + 令牌补全 + 自动化兜底），本清单集中登记这些未纳入的既有债，作为后续收敛依据
> 来源：[约定与代码裁决审查报告](约定与代码裁决审查报告.md) 八条指控裁决

## 说明

清单内各项均以 `check:deprecated` 基线（[.deprecated-utilities-baseline.json](../../packages/ui/scripts/.deprecated-utilities-baseline.json)）或人工清单登记现状；清理过程中运行 `pnpm --filter brutx-ui-vue check:deprecated:update` 逐步收敛基线到零。

## 修复进展（2026-08-12）

| 项 | 状态 | 提交 |
| --- | --- | --- |
| C1 焦点体系残留 outline-none | ✅ 已修复（3 处 a11y 缺口 + 惰性 outline-none 清理 + ring 遗物） | `39ca3cc5` |
| C2 手抄 shadow 值 | ✅ 已修复（10 处迁移令牌，SHADOW_RGBA 基线清零） | `b3ba04b4` |
| C3 硬编码颜色 | ✅ 已修复（Result/Counter/loading/Loading/Tour 回归令牌） | `b3ba04b4` |
| C4 border-3 双定义与细边框 | ✅ 已修复（删手写 `.border-3` + MessageContainer border-3） | `39ca3cc5`、`b3ba04b4` |
| C5 过渡声明与实际属性错配 | ✅ 已修复（5 处日格子 + code-block 复制按钮） | `479f7a3d` |
| C7 command 按压反馈失效 | ✅ 已修复（`data-[highlighted]:active` 复合变体） | `fdeaf170` |
| 可选独立 PR：border-brutal 迁移 @utility | ⏳ 保持待办（评审决策前跳过） | — |
| docs 侧 ring 残留 | ⏳ 待办（范围外） | — |
| 组件文档镜像缺口 | ⏳ 待办（既有债，范围外） | — |

**C6 separator / C8 toggle**：已由前序提交 `0c4abfc8` 修复（完整字面量常量 / `brutalPressedStateOn`），本次补 toggle ON 态类断言（`8e1b3c9c`）。

## C1 焦点体系残留 outline-none

- **状态**：✅ 已修复（`39ca3cc5`）
- **范围**：11 处 `outline-none` 中删除 8 处（含 ring 遗物），豁免 3 处内层 input（input / number-input / TagsInput 的 `focus:outline-none`，容器 focus-within 为唯一指示）
- **依据**：裁决报告 §C1；机制与唯一例外见 [tailwind-v4-mechanisms.md](../guides/tailwind-v4-mechanisms.md) §1
- **收尾**：3 处真实 a11y 缺口已修（CommandInput 容器补 focus-within、cascader trigger 迁 `FOCUS_OUTLINE_CLASSES`、MenuItem/SubMenu 补 focus-visible）；popover 面板/select/command/cascader 列表项惰性 `outline-none` 已删；input-variants 的 `focus:ring-0` 已删。select trigger 的「outline + 位移」双焦点指示维持现状（迁移方向属设计决策，见机会审查报告 §二）

## C2 手抄 shadow 值

- **状态**：✅ 已修复（`b3ba04b4`）
- **范围**：`shadow-[Npx_..._rgba(...)]` 任意值字面量（Calendar / Counter / Counter test / Image(loading) / Loading / MessageContainer / Tour / loading.ts）
- **依据**：[.deprecated-utilities-baseline.json](../../packages/ui/scripts/.deprecated-utilities-baseline.json) 的 SHADOW_RGBA 项；迁移样板参照 Result / Image 已令牌化写法（`shadow-brutal*`）
- **收尾**：2px→`shadow-brutal-sm`、4px→`shadow-brutal`（dark:shadow 冗余移除）；标度外 1px（Calendar 事件标签）保留偏移、颜色走 `var(--brutal-shadow-color)`；counter.test.ts 断言同步。`check:deprecated` SHADOW_RGBA 基线清零

## C3 硬编码颜色

- **状态**：✅ 已修复（`b3ba04b4`）
- **范围**：Tailwind 默认色板与硬编码前景（`text-gray-500`、`bg-[#...]` 等）
- **依据**：裁决报告 §C3；规则见 [VISUAL_SYSTEM](../guides/VISUAL_SYSTEM.md) R6（`--brutal-*` 令牌与 `*-foreground` 家族）
- **收尾**：Result/Counter/loading/Loading/Tour 的 `border-brutal-black`/`bg-white`/`text-gray-500`/`text-brutal-black dark:text-white` 回归 `border-brutal`/`bg-brutal-bg`/`text-brutal-muted-foreground`/`text-brutal-fg`；Tour 虚线边框改 `border-brutal-dashed`。TreeViewNode 虚线指示与 5% 叠色保持例外（裁决报告 §C3）

## C4 border-3 双定义与细边框

- **状态**：✅ 已修复（`39ca3cc5` 删手写 `.border-3` + `b3ba04b4` MessageContainer border-3）
- **范围**：`styles.css` 手写 `.border-3`（与 Tailwind 生成同值冗余）删除；细边框（如 MessageContainer `border-2`）迁移为粗边框
- **依据**：裁决报告 §C4；规则见 [VISUAL_SYSTEM](../guides/VISUAL_SYSTEM.md) R1
- **收尾**：手写 `.border-3` 已删（Tailwind 经 `--border-width-3` 自动生成，产物核验通过）；MessageContainer `border-2`→`border-3` 对齐 toast；Transfer 次级细边框保留（豁免）

## C5 过渡声明与实际属性错配

- **状态**：✅ 已修复（`479f7a3d`）
- **范围**：code-block 复制按钮反馈失效 + 5 处死 `transition-all`
- **依据**：裁决报告 §C5；规则见 [COMPONENT_GUIDE](../guides/COMPONENT_GUIDE.md) r13（同一交互元素只声明一份 transition，twMerge 同组静默移除见 [tailwind-v4-mechanisms.md](../guides/tailwind-v4-mechanisms.md) §2）
- **收尾**：5 处日格子（Calendar + 4 个 date-picker 面板）改 breadcrumb 模型 `transition-[transform,box-shadow,background-color,color]`；code-block 复制按钮窄化 overlay 竞争类，修复 copied/failed 状态反馈。⚠️ 副作用：`brutalPressWithTransition` 随消费者清空转为死导出（更新见机会审查报告 §13.2）

## C7 command 按压反馈失效

- **状态**：✅ 已修复（`fdeaf170`）
- **范围**：command-variants.ts 的 `data-[highlighted]` 与 `brutalPress` 并存，按压反馈被 `data-*` 压过；需写 `data-*:active:...` 复合变体（特异度 `0,3,0`，含 `translate-x-0` 重置）
- **依据**：裁决报告 §C7；字节序机制见 [COMPONENT_GUIDE](../guides/COMPONENT_GUIDE.md)「共享交互变体机制契约」②
- **收尾**：`data-[highlighted]:active:translate-y-[var(--brutal-pressed-offset,2px)]` + `translate-x-0` + `shadow-none` 已补（产物核验通过）

## 可选独立 PR：border-brutal 迁移 @utility

- **范围**：删除手写 `.border-3`，将 `.border-brutal` 移出 `@layer utilities` 转为顶层 `@utility`，复活 `hover:border-brutal` / `data-[highlighted]:border-brutal` 变体（command / menu 悬停边框）
- **注意**：行为变更，需视觉 QA，独立 PR 评审

## docs 侧 ring 残留

- **范围**：ThemePlayground.vue / visual/App.vue 等 docs 侧 `ring-*` 类（check:deprecated 只扫 `packages/ui/src`，不覆盖此面）

## 组件文档镜像缺口

- **范围**：`pnpm check:i18n:strict` 报 51 个 changelog 历史段 en 镜像缺失（`apps/docs/changelog/*.md` 无对应 `apps/docs/en/changelog/*.md`），既有债，不在约定体系修复范围
